[project]
name = "llm-study"
version = "0.1.0"
description = "Deep dive em embeddings, LLMs e AI Agents"
requires-python = ">=3.10"

dependencies = [
    "torch>=2.0.0",
    "transformers>=4.30.0",
    "Pillow>=10.0.0",
    "matplotlib>=3.7.0",
    "jupyter>=1.0.0",
    "ipywidgets>=8.0.0",
    "numpy>=1.24.0",
]

[tool.uv]
dev-dependencies = []
